{
  "name": "geoicons-distribution",
  "version": "1.0.0",
  "description": "GeoIcons — premium geographic map icons for React, Vue, Angular, and vanilla JS. Dual-licensed GPLv3 or Commercial.",
  "private": true,
  "type": "commonjs",
  "license": "GPL-3.0-only",
  "homepage": "https://geoicons.io",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/getgeoicons/geoicons.git"
  },
  "bugs": {
    "url": "https://github.com/getgeoicons/geoicons/issues"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --prefix packages/core run build && npm --prefix packages/react run build && npm --prefix packages/vue run build && npm --prefix packages/angular run build && npm --prefix packages/vanilla run build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit -p packages/react/tsconfig.json && tsc --noEmit -p packages/vue/tsconfig.json && tsc --noEmit -p packages/vanilla/tsconfig.json"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^6.0.3"
  }
}
